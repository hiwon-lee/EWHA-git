// 한수
#include <iostream>
#include <algorithm>
#include <vector>
using namespace std;

bool isLimit(int limit, int curr)
{
    bool limit = false;
    if (limit < curr)
        true;
    return limit;
}

// dp 문제 푸는것마냥 풀어야 돼
int main()
{
    int n, cnt = 0;
    cin >> n;
    vector<int> arr(n);

    // 몆자리 수인가
    int volumn = n / 10;

    while (volumn)
    {

        // int diff =

        if (isLimit)
        {
            break;
        }
    }

    cout << cnt;
}